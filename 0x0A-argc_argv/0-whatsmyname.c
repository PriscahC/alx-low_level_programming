#include <stdio.h>

/**
 * main - entry point of the code
 * Description - parameters in main function
 * @argc - counts number of arguments
 * @argv - contains the command line arguments
 * Return - program returns zero
 */

int main (int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
