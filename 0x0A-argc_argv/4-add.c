#include <stdio.h>
#include <stdlib.h>

/**
 * main - code entry point
 * description - adds argv 
 * @argc: argument count
 * @argv: argument vector
 * Return: returns zero
 */

int main(int argc, char *argv[])
{
	for (i = 0; i < argc; i++)
	{
		if 
	}
